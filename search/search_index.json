{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"nhentai","text":"<p>A Python library to download nhentai galleries with optional manga translation and PDF export.</p>"},{"location":"#features","title":"Features","text":"<ul> <li>Parallel downloads with configurable worker threads</li> <li>Translation via NekoTranslate, with automatic proxy rotation for anonymous use</li> <li>PDF export \u2014 compile a gallery into a single file</li> <li>Type-safe <code>Engine</code> and <code>Language</code> enums with full IDE autocomplete</li> <li>CLI \u2014 usable without writing code</li> </ul>"},{"location":"#installation","title":"Installation","text":"pip install git+https://github.com/decryptable/nhentai.gitSuccessfully installed nhentai-0.1.2 <p>With optional extras:</p> # Everythingpip install \"nhentai[all] @ git+https://github.com/decryptable/nhentai.git\"Successfully installed nhentai-0.1.2"},{"location":"#quick-start","title":"Quick start","text":"<pre><code>from nhentai import NHentai, Downloader\n\ngallery = NHentai(\"639456\")\nprint(gallery.title[\"pretty\"])\n\ndl = Downloader(gallery, output_dir=\"./downloads\")\npaths = dl.download()\n</code></pre> <p>See Getting Started for more examples.</p>"},{"location":"changelog/","title":"Changelog","text":""},{"location":"changelog/#013-2026-03-26","title":"0.1.3 \u2014 2026-03-26","text":""},{"location":"changelog/#added","title":"Added","text":"<ul> <li>Integrated termynal.py for interactive CLI animations in documentation.</li> </ul>"},{"location":"changelog/#changed","title":"Changed","text":"<ul> <li>Updated version to 0.1.3</li> </ul>"},{"location":"changelog/#012-2026-03-26","title":"0.1.2 \u2014 2026-03-26","text":""},{"location":"changelog/#added_1","title":"Added","text":"<ul> <li>New Comic-Translator provider supporting <code>/v3</code> API.</li> <li>Direct URL translation in <code>Downloader</code> (much faster as it avoids re-uploading).</li> <li>Interactive mode (<code>-i</code>) for CLI with provider/engine selection.</li> <li><code>Provider</code> enum to separate translation source from AI engine.</li> <li><code>ComicEngine</code> and <code>NekoEngine</code> for provider-specific model selection.</li> </ul>"},{"location":"changelog/#changed_1","title":"Changed","text":"<ul> <li>Refactored <code>nhentai/translator.py</code> into <code>nhentai/providers/</code> package for better maintainability.</li> <li>Updated <code>Downloader</code> to automatically attempt the fastest translation method first.</li> <li>Fixed <code>ruff</code> linting and improved test coverage to 80%+.</li> </ul>"},{"location":"changelog/#011-2026-03-26","title":"0.1.1 \u2014 2026-03-26","text":""},{"location":"changelog/#added_2","title":"Added","text":"<ul> <li>Support for proxy lists from both local files and raw URLs in CLI</li> <li>Enhanced <code>parse_proxy_list</code> function to detect and fetch URL-based proxy lists</li> <li>Comprehensive test coverage for new proxy list functionality</li> </ul>"},{"location":"changelog/#changed_2","title":"Changed","text":"<ul> <li>Updated version to 0.1.1</li> </ul>"},{"location":"changelog/#010-2026-03-26","title":"0.1.0 \u2014 2026-03-26","text":"<p>Initial release.</p> <ul> <li><code>NHentai</code> \u2014 gallery metadata via nhentai HTML scraping</li> <li><code>Downloader</code> \u2014 parallel page download, optional translation, optional PDF export</li> <li><code>NekoTranslator</code> \u2014 NekoTranslate API client with automatic proxy rotation</li> <li><code>Engine</code> / <code>Language</code> \u2014 type-safe str enums</li> <li>CLI via <code>nhentai</code> command</li> </ul>"},{"location":"getting-started/","title":"Getting Started","text":""},{"location":"getting-started/#installation","title":"Installation","text":"pip install git+https://github.com/decryptable/nhentai.gitSuccessfully installed nhentai-0.1.2"},{"location":"getting-started/#basic-download","title":"Basic download","text":"<pre><code>from nhentai import NHentai, Downloader\n\ngallery = NHentai(\"639456\")          # accepts URL or bare ID\nprint(gallery.title[\"pretty\"])\nprint(f\"{gallery.num_pages} pages, {gallery.num_favorites} favorites\")\n\ndl = Downloader(gallery, output_dir=\"./downloads\", workers=8)\npaths = dl.download()\nprint(f\"Saved {len(paths)} pages to {paths[0].parent}\")\n</code></pre>"},{"location":"getting-started/#with-translation","title":"With translation","text":"<p>The library supports multiple translation providers.</p>"},{"location":"getting-started/#nekotranslate-default","title":"NekoTranslate (Default)","text":"<p>Anonymous mode is free but quota is per-IP. The library rotates proxies automatically.</p> <pre><code>from nhentai import NHentai, Downloader, NekoTranslator, NekoEngine, Language\n\ngallery = NHentai(\"639456\")\ntranslator = NekoTranslator()\n\ndl = Downloader(\n    gallery,\n    output_dir=\"./downloads\",\n    translator=translator,\n    translate_lang=Language.INDONESIAN,\n    translate_engine=NekoEngine.DEEPL,\n)\npaths = dl.download()\n</code></pre>"},{"location":"getting-started/#comic-translator","title":"Comic-Translator","text":"<p>Excellent for manga-style translation with visual text replacement. Faster via direct URL translation.</p> <pre><code>from nhentai import NHentai, Downloader, ComicTranslator, ComicEngine, Language\n\ngallery = NHentai(\"639456\")\ntranslator = ComicTranslator() # auto guest account\n\ndl = Downloader(\n    gallery,\n    output_dir=\"./downloads\",\n    translator=translator,\n    translate_lang=Language.ENGLISH,\n    translate_engine=ComicEngine.GPT5_MINI,\n)\npaths = dl.download()\n</code></pre>"},{"location":"getting-started/#export-to-pdf","title":"Export to PDF","text":"<pre><code>pdf_path = dl.download(make_pdf=True)[0]\nprint(f\"PDF saved to {pdf_path}\")\n</code></pre> <p>PDF export requires <code>Pillow</code>.</p> pip install \"nhentai[pdf] @ git+https://github.com/decryptable/nhentai.git\"Successfully installed Pillow-10.2.0"},{"location":"getting-started/#cli","title":"CLI","text":""},{"location":"getting-started/#basic-download_1","title":"Basic Download","text":"nhentai 639456[Neko's Adventure] (24 pages)Downloading... 1/24Downloading... 12/24Downloading... 24/24Done. Saved to: ./639456/639456.pdf"},{"location":"getting-started/#with-translation-neko","title":"With Translation (Neko)","text":"nhentai 639456 --translate --lang id --engine deepl[Neko's Adventure] (24 pages)Translating (deepl)... 1/24Translating (deepl)... 12/24Translating (deepl)... 24/24Done. Saved to: ./639456/639456.pdf"},{"location":"getting-started/#with-translation-comic-translator","title":"With Translation (Comic-Translator)","text":"nhentai 639456 --translate --provider comic --engine GPT-5-mini[Neko's Adventure] (24 pages)Translating (GPT-5-mini)... 1/24Translating (GPT-5-mini)... 24/24Done. Saved to: ./639456/639456.pdf"},{"location":"getting-started/#interactive-mode","title":"Interactive Mode","text":"nhentai -iInteractive ModeEnter gallery URL or ID: 639456Output directory (.): .Parallel download workers (4): 8Translate pages? (y/n): yTranslation provider (neko/comic): nekoTarget language (en/id/jp/zh): idTranslation engine (google_cloud/deepl/auto/deepseekv31/gpt5_mini/claude45_sonnet/gpt5): deeplNekoTranslate Bearer token (leave empty to skip): Generate PDF? (y/n): yEnable verbose logging? (y/n): nUse proxy? (y/n): n[Neko's Adventure] (24 pages)Done. Saved to: ./639456/639456.pdf"},{"location":"getting-started/#help","title":"Help","text":"nhentai --helpusage: nhentai [-h] [--version] [url] [-o OUTPUT] [-w WORKERS] [--translate]                [--provider {neko,comic}] [--lang {en,id,jp,zh}]               [--engine ENGINE] [--token TOKEN] [--no-pdf] [--interactive]nhentai downloader with optional translation"},{"location":"getting-started/#engines","title":"Engines","text":""},{"location":"getting-started/#nekoengine","title":"NekoEngine","text":"Enum Value Name Auth required <code>NekoEngine.GOOGLE</code> <code>google_cloud</code> Google Translate No <code>NekoEngine.DEEPL</code> <code>deepl</code> DeepL No <code>NekoEngine.AUTO</code> <code>auto</code> Auto-select No <code>NekoEngine.DEEPSEEK</code> <code>deepseekv31</code> DeepSeek-V3.1 Yes <code>NekoEngine.GPT5_MINI</code> <code>gpt5_mini</code> GPT-5 mini Yes <code>NekoEngine.CLAUDE</code> <code>claude45_sonnet</code> Claude Sonnet 4.5 Yes <code>NekoEngine.GPT5</code> <code>gpt5</code> GPT-5 Yes"},{"location":"getting-started/#comicengine","title":"ComicEngine","text":"Enum Value Name <code>ComicEngine.GEMINI_FLASH</code> <code>Gemini2.5Flash</code> Gemini 2.5 Flash <code>ComicEngine.GPT5_MINI</code> <code>GPT-5-mini</code> GPT-5.1 mini <code>ComicEngine.GPT5</code> <code>GPT-5.1</code> GPT-5.1 <code>ComicEngine.DEEPSEEK</code> <code>Deepseek</code> Deepseek <code>ComicEngine.GROK</code> <code>Grok4fast</code> Grok v4 fast <code>ComicEngine.KIMI</code> <code>KimiK2</code> Kimi K2"},{"location":"api/","title":"API Reference","text":"<p>This section is auto-generated from docstrings in the source code.</p>"},{"location":"api/#modules","title":"Modules","text":"Module Description <code>NHentai</code> Fetch and parse gallery metadata <code>Downloader</code> Download pages with optional translation and PDF export <code>NekoTranslator</code> NekoTranslate API client <code>Engine</code> / <code>Language</code> Type-safe enums for engine and language selection <code>CDN</code> CDN URL builder <code>Grabber</code> Low-level HTTP fetcher <code>Parser</code> HTML parser for gallery JSON"},{"location":"api/cdn/","title":"CDN","text":"<p>Builds nhentai CDN image URLs. Host is selected randomly from the pool on each call.</p>"},{"location":"api/cdn/#nhentai.cdn.CDN","title":"<code>nhentai.cdn.CDN</code>","text":"<p>Builds nhentai CDN image URLs with random host selection for load balancing.</p>"},{"location":"api/cdn/#nhentai.cdn.CDN.get_image_url","title":"<code>get_image_url(media_id: int, page: int, ext: str = 'webp') -&gt; str</code>","text":"<p>Full-resolution page URL.</p> Source code in <code>nhentai/cdn.py</code> <pre><code>def get_image_url(self, media_id: int, page: int, ext: str = \"webp\") -&gt; str:\n    \"\"\"Full-resolution page URL.\"\"\"\n    host = random.choice(self._image_hosts)\n    return f\"https://{host}/galleries/{media_id}/{page}.{ext}\"\n</code></pre>"},{"location":"api/cdn/#nhentai.cdn.CDN.get_thumb_url","title":"<code>get_thumb_url(media_id: int, page: int, ext: str = 'webp') -&gt; str</code>","text":"<p>Thumbnail URL for a specific page.</p> Source code in <code>nhentai/cdn.py</code> <pre><code>def get_thumb_url(self, media_id: int, page: int, ext: str = \"webp\") -&gt; str:\n    \"\"\"Thumbnail URL for a specific page.\"\"\"\n    host = random.choice(self._thumb_hosts)\n    return f\"https://{host}/galleries/{media_id}/{page}t.{ext}\"\n</code></pre>"},{"location":"api/cdn/#nhentai.cdn.CDN.get_cover_url","title":"<code>get_cover_url(media_id: int, ext: str = 'webp') -&gt; str</code>","text":"<p>Cover image URL.</p> Source code in <code>nhentai/cdn.py</code> <pre><code>def get_cover_url(self, media_id: int, ext: str = \"webp\") -&gt; str:\n    \"\"\"Cover image URL.\"\"\"\n    host = random.choice(self._thumb_hosts)\n    return f\"https://{host}/galleries/{media_id}/cover.{ext}\"\n</code></pre>"},{"location":"api/cdn/#nhentai.cdn.CDN.ext_from_type","title":"<code>ext_from_type(t: str) -&gt; str</code>  <code>staticmethod</code>","text":"<p>Convert nhentai page type code (<code>j</code>/<code>p</code>/<code>w</code>/<code>g</code>) to file extension.</p> Source code in <code>nhentai/cdn.py</code> <pre><code>@staticmethod\ndef ext_from_type(t: str) -&gt; str:\n    \"\"\"Convert nhentai page type code (``j``/``p``/``w``/``g``) to file extension.\"\"\"\n    return EXT_MAP.get(t, \"jpg\")\n</code></pre>"},{"location":"api/downloader/","title":"Downloader","text":"<p>Downloads pages from a gallery with optional per-page translation and PDF export.</p> <pre><code>from nhentai import NHentai, Downloader, NekoTranslator, Engine, Language\n\ngallery = NHentai(\"639456\")\n\n# Download only\ndl = Downloader(gallery, output_dir=\"./downloads\", workers=8)\npaths = dl.download()\n\n# Download + translate + PDF\ntranslator = NekoTranslator()\ndl = Downloader(\n    gallery,\n    output_dir=\"./downloads\",\n    translator=translator,\n    translate_lang=Language.INDONESIAN,\n    translate_engine=Engine.DEEPL,\n)\npdf = dl.download(make_pdf=True)\n</code></pre> <p>If a page fails to translate, the original image is saved instead (silent fallback).</p>"},{"location":"api/downloader/#proxy-support","title":"Proxy Support","text":"<p>The downloader supports proxy lists for enhanced connectivity. Proxy lists can be provided as local files or URLs.</p>"},{"location":"api/downloader/#using-local-proxy-list-file","title":"Using Local Proxy List File","text":"<pre><code>dl = Downloader(\n    gallery,\n    output_dir=\"./downloads\",\n    proxy_list=[\"http://127.0.0.1:8080\", \"socks5://10.0.0.1:1080\"]\n)\n</code></pre>"},{"location":"api/downloader/#using-url-based-proxy-list","title":"Using URL-based Proxy List","text":"<pre><code>dl = Downloader(\n    gallery,\n    output_dir=\"./downloads\",\n    proxy_list=[\"https://example.com/proxies.txt\"]\n)\n</code></pre> <p>The downloader will automatically fetch proxy lists from URLs and use them for requests.</p>"},{"location":"api/downloader/#nhentai.downloader.Downloader","title":"<code>nhentai.downloader.Downloader(gallery: NHentai, output_dir: str | Path = '.', workers: int = 4, translator: Any | None = None, translate_lang: Language | str = Language.ENGLISH, translate_engine: str | Any = 'deepl', timeout: int = 30, proxy_list: list[str] | None = None)</code>","text":"<p>Downloads pages from an :class:<code>NHentai</code> gallery, with optional translation and PDF export.</p> <p>Parameters:</p> Name Type Description Default <code>gallery</code> <code>NHentai</code> <p>Parsed gallery object.</p> required <code>output_dir</code> <code>str | Path</code> <p>Root directory for downloads. Gallery pages go into <code>&lt;output_dir&gt;/&lt;gallery_id&gt;/</code>.</p> <code>'.'</code> <code>workers</code> <code>int</code> <p>Number of parallel download threads.</p> <code>4</code> <code>translator</code> <code>Any | None</code> <p>Optional :class:<code>NekoTranslator</code>. If provided, each page is translated before saving.</p> <code>None</code> <code>translate_lang</code> <code>Language | str</code> <p>Target language for translation.</p> <code>ENGLISH</code> <code>translate_engine</code> <code>str | Any</code> <p>Engine to use for translation.</p> <code>'deepl'</code> <code>timeout</code> <code>int</code> <p>Per-request timeout in seconds.</p> <code>30</code> Source code in <code>nhentai/downloader.py</code> <pre><code>def __init__(\n    self,\n    gallery: NHentai,\n    output_dir: str | Path = \".\",\n    workers: int = 4,\n    translator: Any | None = None,\n    translate_lang: Language | str = Language.ENGLISH,\n    translate_engine: str | Any = \"deepl\",\n    timeout: int = 30,\n    proxy_list: list[str] | None = None,\n):\n    self.gallery = gallery\n    self.output_dir = Path(output_dir)\n    self.workers = max(1, workers)\n    self.translator: Any | None = translator\n    self.translate_lang = Language(translate_lang) if not isinstance(translate_lang, Language) else translate_lang\n    self.translate_engine = translate_engine\n    self.timeout = timeout\n    self.proxy_list = proxy_list or []\n</code></pre>"},{"location":"api/downloader/#nhentai.downloader.Downloader.download","title":"<code>download(make_pdf: bool = False, progress_callback: Callable[[int, int, str], None] | None = None) -&gt; list[Path]</code>","text":"<p>Download all pages. Returns a list of saved file paths (or a single PDF path if <code>make_pdf=True</code>).</p> <p>Parameters:</p> Name Type Description Default <code>make_pdf</code> <code>bool</code> <p>Compile pages into a PDF after downloading. Requires Pillow.</p> <code>False</code> <code>progress_callback</code> <code>Callable[[int, int, str], None] | None</code> <p>Optional callback to report progress: (current, total, status).</p> <code>None</code> <p>Raises:</p> Type Description <code>DownloadError</code> <p>If a page fails to download.</p> <code>RuntimeError</code> <p>If <code>make_pdf=True</code> but Pillow is not installed.</p> Source code in <code>nhentai/downloader.py</code> <pre><code>def download(self, make_pdf: bool = False, progress_callback: Callable[[int, int, str], None] | None = None) -&gt; list[Path]:\n    \"\"\"\n    Download all pages. Returns a list of saved file paths (or a single PDF path if ``make_pdf=True``).\n\n    :param make_pdf: Compile pages into a PDF after downloading. Requires Pillow.\n    :param progress_callback: Optional callback to report progress: (current, total, status).\n    :raises DownloadError: If a page fails to download.\n    :raises RuntimeError: If ``make_pdf=True`` but Pillow is not installed.\n    \"\"\"\n    import concurrent.futures\n\n    gallery_target_dir = self.output_dir / str(self.gallery.id)\n\n    with tempfile.TemporaryDirectory() as tmp_dir:\n        tmp_path = Path(tmp_dir)\n        urls = self.gallery.get_image_urls()\n        results: list[Path | None] = [None] * len(urls)\n        total = len(urls)\n\n        def _update_progress(current: int, status: str):\n            if progress_callback:\n                progress_callback(current, total, status)\n\n        with concurrent.futures.ThreadPoolExecutor(max_workers=self.workers) as pool:\n            futures = {\n                pool.submit(self._download_page, url, tmp_path, i + 1, _update_progress): i\n                for i, url in enumerate(urls)\n            }\n            for future in concurrent.futures.as_completed(futures):\n                idx = futures[future]\n                try:\n                    results[idx] = future.result()\n                except Exception as exc:\n                    # If one fails, we might want to cancel others or just let them finish/error\n                    raise DownloadError(f\"Failed to download page {idx + 1}: {exc}\") from exc\n\n        paths = [p for p in results if p is not None]\n\n        if make_pdf:\n            self.output_dir.mkdir(parents=True, exist_ok=True)\n            pdf_path = self._make_pdf(paths, self.output_dir)\n            return [pdf_path]\n\n        # If not PDF, move from temp to final destination\n        gallery_target_dir.mkdir(parents=True, exist_ok=True)\n        final_paths = []\n        for p in paths:\n            target = gallery_target_dir / p.name\n            shutil.move(str(p), str(target))\n            final_paths.append(target)\n        return final_paths\n</code></pre>"},{"location":"api/downloader/#nhentai.downloader.DownloadError","title":"<code>nhentai.downloader.DownloadError</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Raised when a page download fails.</p>"},{"location":"api/enums/","title":"Enums","text":"<p><code>NekoEngine</code>, <code>ComicEngine</code>, <code>Provider</code> and <code>Language</code> are <code>str</code> subclasses, so they work transparently as plain strings in any context (requests, JSON, <code>str</code> comparisons).</p> <pre><code>from nhentai import NekoEngine, ComicEngine, Provider, Language\n\n# Both forms are valid anywhere an engine/language is accepted\nNekoEngine.DEEPL == \"deepl\"          # True\nProvider.COMIC == \"comic\"            # True\nLanguage(\"id\") is Language.INDONESIAN  # True\n</code></pre>"},{"location":"api/enums/#nhentai.enums.Provider","title":"<code>nhentai.enums.Provider</code>","text":"<p>               Bases: <code>StrEnum</code></p> <p>Translation provider.</p>"},{"location":"api/enums/#nhentai.enums.NekoEngine","title":"<code>nhentai.enums.NekoEngine</code>","text":"<p>               Bases: <code>StrEnum</code></p> <p>NekoTranslate engines.</p>"},{"location":"api/enums/#nhentai.enums.ComicEngine","title":"<code>nhentai.enums.ComicEngine</code>","text":"<p>               Bases: <code>StrEnum</code></p> <p>ComicTranslator engines.</p>"},{"location":"api/enums/#nhentai.enums.Language","title":"<code>nhentai.enums.Language</code>","text":"<p>               Bases: <code>StrEnum</code></p> <p>ISO 639-1 target language codes supported by NekoTranslate.</p>"},{"location":"api/grabber/","title":"Grabber","text":"<p>Low-level HTTP client with nhentai-compatible headers. Used internally by <code>NHentai</code>, but available if you need raw HTML or image bytes.</p>"},{"location":"api/grabber/#nhentai.grabber.Grabber","title":"<code>nhentai.grabber.Grabber(url: str, timeout: int = 30, headers: dict | None = None)</code>","text":"<p>Fetches raw HTML or image bytes from a URL with nhentai-compatible headers.</p> <p>Parameters:</p> Name Type Description Default <code>url</code> <code>str</code> <p>Target URL.</p> required <code>timeout</code> <code>int</code> <p>Request timeout in seconds.</p> <code>30</code> <code>headers</code> <code>dict | None</code> <p>Additional headers merged with defaults.</p> <code>None</code> Source code in <code>nhentai/grabber.py</code> <pre><code>def __init__(self, url: str, timeout: int = 30, headers: dict | None = None):\n    \"\"\"\n    :param url: Target URL.\n    :param timeout: Request timeout in seconds.\n    :param headers: Additional headers merged with defaults.\n    \"\"\"\n    self.url = url\n    self.timeout = timeout\n    self.headers = {**_DEFAULT_HEADERS, **(headers or {})}\n</code></pre>"},{"location":"api/grabber/#nhentai.grabber.Grabber.get_html","title":"<code>get_html() -&gt; str</code>","text":"<p>Fetch and return the HTML of :attr:<code>url</code>.</p> <p>Raises:</p> Type Description <code>ConnectionError</code> <p>On HTTP error, timeout, or network failure.</p> Source code in <code>nhentai/grabber.py</code> <pre><code>def get_html(self) -&gt; str:\n    \"\"\"Fetch and return the HTML of :attr:`url`.\n\n    :raises ConnectionError: On HTTP error, timeout, or network failure.\n    \"\"\"\n    try:\n        resp = requests.get(\n            self.url, headers=self.headers, verify=False,\n            timeout=self.timeout, allow_redirects=True,\n        )\n        resp.raise_for_status()\n        return resp.text\n    except requests.exceptions.HTTPError as exc:\n        raise ConnectionError(f\"HTTP {exc.response.status_code} fetching {self.url}\") from exc\n    except requests.exceptions.Timeout as exc:\n        raise ConnectionError(f\"Request timed out after {self.timeout}s: {self.url}\") from exc\n    except requests.exceptions.ConnectionError as exc:\n        raise ConnectionError(f\"Network error fetching {self.url}: {exc}\") from exc\n</code></pre>"},{"location":"api/grabber/#nhentai.grabber.Grabber.download_bytes","title":"<code>download_bytes(url: str) -&gt; bytes</code>","text":"<p>Download and return raw bytes from url.</p> <p>Raises:</p> Type Description <code>ConnectionError</code> <p>On HTTP error, timeout, or network failure.</p> Source code in <code>nhentai/grabber.py</code> <pre><code>def download_bytes(self, url: str) -&gt; bytes:\n    \"\"\"Download and return raw bytes from *url*.\n\n    :raises ConnectionError: On HTTP error, timeout, or network failure.\n    \"\"\"\n    try:\n        resp = requests.get(\n            url, headers=self.headers, verify=False,\n            timeout=self.timeout, stream=True,\n        )\n        resp.raise_for_status()\n        return resp.content\n    except requests.exceptions.HTTPError as exc:\n        raise ConnectionError(f\"HTTP {exc.response.status_code} downloading {url}\") from exc\n    except requests.exceptions.Timeout as exc:\n        raise ConnectionError(f\"Download timed out: {url}\") from exc\n    except requests.exceptions.ConnectionError as exc:\n        raise ConnectionError(f\"Network error downloading {url}: {exc}\") from exc\n</code></pre>"},{"location":"api/nhentai/","title":"NHentai","text":"<p>Fetches and exposes metadata for a single nhentai gallery.</p> <p>Accepts either a full URL or a bare gallery ID:</p> <pre><code>from nhentai import NHentai\n\ngallery = NHentai(\"639456\")\ngallery = NHentai(\"https://nhentai.net/g/639456/\")\n</code></pre>"},{"location":"api/nhentai/#nhentai.nhentai.NHentai","title":"<code>nhentai.nhentai.NHentai(url: str)</code>","text":"<p>Fetches and exposes metadata for a single nhentai gallery.</p> <p>Accepts either a full URL (<code>https://nhentai.net/g/639456/</code>) or a bare gallery ID (<code>\"639456\"</code>).</p> <p>Parameters:</p> Name Type Description Default <code>url</code> <code>str</code> <p>Gallery URL or numeric ID string.</p> required <p>Raises:</p> Type Description <code>ConnectionError</code> <p>If the page cannot be fetched.</p> <code>ValueError</code> <p>If gallery data cannot be parsed.</p> Source code in <code>nhentai/nhentai.py</code> <pre><code>def __init__(self, url: str):\n    \"\"\"\n    :param url: Gallery URL or numeric ID string.\n    :raises ConnectionError: If the page cannot be fetched.\n    :raises ValueError: If gallery data cannot be parsed.\n    \"\"\"\n    if not url.startswith(\"http\"):\n        url = f\"{self.BASE_URL}/g/{url.strip('/')}\"\n    self.url = url\n    self._grabber = Grabber(url)\n    self._cdn = CDN()\n    self._info = Parser(self._grabber.get_html()).parse_gallery_info()\n</code></pre>"},{"location":"api/nhentai/#nhentai.nhentai.NHentai.id","title":"<code>id: int</code>  <code>property</code>","text":"<p>Gallery ID.</p>"},{"location":"api/nhentai/#nhentai.nhentai.NHentai.media_id","title":"<code>media_id: int</code>  <code>property</code>","text":"<p>Internal media ID used in CDN URLs.</p>"},{"location":"api/nhentai/#nhentai.nhentai.NHentai.title","title":"<code>title: dict</code>  <code>property</code>","text":"<p>Dict with keys <code>english</code>, <code>japanese</code>, <code>pretty</code>.</p>"},{"location":"api/nhentai/#nhentai.nhentai.NHentai.num_pages","title":"<code>num_pages: int</code>  <code>property</code>","text":"<p>Total number of pages.</p>"},{"location":"api/nhentai/#nhentai.nhentai.NHentai.num_favorites","title":"<code>num_favorites: int</code>  <code>property</code>","text":"<p>Number of times this gallery has been favorited.</p>"},{"location":"api/nhentai/#nhentai.nhentai.NHentai.upload_date","title":"<code>upload_date: int</code>  <code>property</code>","text":"<p>Upload timestamp (Unix epoch).</p>"},{"location":"api/nhentai/#nhentai.nhentai.NHentai.tags","title":"<code>tags: list[dict]</code>  <code>property</code>","text":"<p>Tags of type <code>tag</code>.</p>"},{"location":"api/nhentai/#nhentai.nhentai.NHentai.artists","title":"<code>artists: list[dict]</code>  <code>property</code>","text":"<p>Tags of type <code>artist</code>.</p>"},{"location":"api/nhentai/#nhentai.nhentai.NHentai.groups","title":"<code>groups: list[dict]</code>  <code>property</code>","text":"<p>Tags of type <code>group</code>.</p>"},{"location":"api/nhentai/#nhentai.nhentai.NHentai.languages","title":"<code>languages: list[dict]</code>  <code>property</code>","text":"<p>Tags of type <code>language</code>.</p>"},{"location":"api/nhentai/#nhentai.nhentai.NHentai.categories","title":"<code>categories: list[dict]</code>  <code>property</code>","text":"<p>Tags of type <code>category</code>.</p>"},{"location":"api/nhentai/#nhentai.nhentai.NHentai.pages_info","title":"<code>pages_info: list[dict]</code>  <code>property</code>","text":"<p>Raw page info list from gallery JSON (contains <code>t</code>, <code>w</code>, <code>h</code> per page).</p>"},{"location":"api/nhentai/#nhentai.nhentai.NHentai.get_image_urls","title":"<code>get_image_urls() -&gt; list[str]</code>","text":"<p>Full-resolution image URLs for all pages, in order.</p> Source code in <code>nhentai/nhentai.py</code> <pre><code>def get_image_urls(self) -&gt; list[str]:\n    \"\"\"Full-resolution image URLs for all pages, in order.\"\"\"\n    return [\n        self._cdn.get_image_url(\n            self.media_id, i + 1,\n            ext=CDN.ext_from_type(page[\"t\"])\n        )\n        for i, page in enumerate(self.pages_info)\n    ]\n</code></pre>"},{"location":"api/nhentai/#nhentai.nhentai.NHentai.get_cover_url","title":"<code>get_cover_url() -&gt; str</code>","text":"<p>Cover image URL.</p> Source code in <code>nhentai/nhentai.py</code> <pre><code>def get_cover_url(self) -&gt; str:\n    \"\"\"Cover image URL.\"\"\"\n    ext = CDN.ext_from_type(self._info[\"images\"][\"cover\"][\"t\"])\n    return self._cdn.get_cover_url(self.media_id, ext)\n</code></pre>"},{"location":"api/nhentai/#nhentai.nhentai.NHentai.info","title":"<code>info() -&gt; dict</code>","text":"<p>Raw gallery info dict as returned by the API.</p> Source code in <code>nhentai/nhentai.py</code> <pre><code>def info(self) -&gt; dict:\n    \"\"\"Raw gallery info dict as returned by the API.\"\"\"\n    return self._info\n</code></pre>"},{"location":"api/parser/","title":"Parser","text":"<p>Extracts gallery JSON from an nhentai HTML page. Used internally by <code>NHentai</code>.</p>"},{"location":"api/parser/#nhentai.parser.Parser","title":"<code>nhentai.parser.Parser(html: str)</code>","text":"<p>Extracts gallery JSON from an nhentai HTML page.</p> Source code in <code>nhentai/parser.py</code> <pre><code>def __init__(self, html: str):\n    self.soup = BeautifulSoup(html, \"html.parser\")\n</code></pre>"},{"location":"api/parser/#nhentai.parser.Parser.parse_gallery_info","title":"<code>parse_gallery_info() -&gt; dict</code>","text":"<p>Parse and return the gallery info dict from the embedded <code>window._gallery</code> script.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the script tag is missing or JSON is malformed.</p> Source code in <code>nhentai/parser.py</code> <pre><code>def parse_gallery_info(self) -&gt; dict:\n    \"\"\"\n    Parse and return the gallery info dict from the embedded ``window._gallery`` script.\n\n    :raises ValueError: If the script tag is missing or JSON is malformed.\n    \"\"\"\n    script = self.soup.find(\n        \"script\",\n        string=lambda t: t and \"window._gallery = JSON.parse(\" in t,\n    )\n    if not script:\n        raise ValueError(\"Gallery data not found. Page may require login or structure has changed.\")\n\n    raw = script.string.split(\"window._gallery = JSON.parse(\")[1].split(\");\")[0]\n    try:\n        return loads(loads(raw))\n    except (JSONDecodeError, ValueError) as exc:\n        raise ValueError(f\"Failed to parse gallery JSON: {exc}\") from exc\n</code></pre>"},{"location":"api/translator/","title":"Translator","text":"<p>The library provides multiple translation providers that implement the <code>BaseTranslator</code> interface.</p>"},{"location":"api/translator/#nekotranslator","title":"NekoTranslator","text":"<p>Client for the NekoTranslate API. Supports anonymous mode with automatic proxy rotation.</p> <pre><code>from nhentai import NekoTranslator, NekoEngine, Language\n\ntranslator = NekoTranslator(token=\"YOUR_BEARER_TOKEN\") # token is optional\ndata = translator.translate_file(\"page.jpg\", tgt_lang=Language.INDONESIAN, engine=NekoEngine.DEEPL)\n</code></pre>"},{"location":"api/translator/#nhentai.translator.NekoTranslator","title":"<code>nhentai.translator.NekoTranslator(token: str | None = None, proxy_list_url: str | None = None, proxies: list[str] | None = None)</code>","text":"<p>               Bases: <code>BaseTranslator</code></p> <p>NekoTranslate API client.</p> <p>Anonymous (no token): uses free quota per IP, rotates proxies automatically. Authenticated: pass token from nhentai.net localStorage.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str | None</code> <p>Bearer token for authenticated requests (optional).</p> <code>None</code> <code>proxy_list_url</code> <code>str | None</code> <p>URL to a plain-text proxy list. Defaults to proxifly socks5 list.</p> <code>None</code> <code>proxies</code> <code>list[str] | None</code> <p>Explicit proxy list (overrides proxy_list_url).</p> <code>None</code> Source code in <code>nhentai/providers/neko.py</code> <pre><code>def __init__(\n    self,\n    token: str | None = None,\n    proxy_list_url: str | None = None,\n    proxies: list[str] | None = None,\n):\n    \"\"\"\n    :param token: Bearer token for authenticated requests (optional).\n    :param proxy_list_url: URL to a plain-text proxy list. Defaults to proxifly socks5 list.\n    :param proxies: Explicit proxy list (overrides proxy_list_url).\n    \"\"\"\n    self.token = token\n    self.headers: dict[str, str] = {}\n    if self.token:\n        self.headers[\"Authorization\"] = f\"Bearer {self.token}\"\n\n    raw = proxies or self._fetch_proxies(proxy_list_url)\n    self._proxy_list = [_parse_proxy(p) for p in raw if p.strip()]\n    self._proxy_cycle = cycle(self._proxy_list) if self._proxy_list else None\n</code></pre>"},{"location":"api/translator/#nhentai.translator.NekoTranslator.find_proxy_with_quota","title":"<code>find_proxy_with_quota() -&gt; dict | None</code>","text":"<p>Returns the first proxy with remaining anonymous quota, or None.</p> Source code in <code>nhentai/providers/neko.py</code> <pre><code>def find_proxy_with_quota(self) -&gt; dict | None:\n    \"\"\"Returns the first proxy with remaining anonymous quota, or None.\"\"\"\n    for proxy in self._proxy_list:\n        try:\n            resp = requests.get(\n                f\"{BASE_URL}/get-quota\",\n                proxies=proxy,\n                verify=False,\n                timeout=_QUOTA_SCAN_TIMEOUT,\n            )\n            if resp.status_code == 200 and resp.json().get(\"quota\", 0) &gt; 0:\n                return proxy\n        except requests.exceptions.RequestException:\n            continue\n    return None\n</code></pre>"},{"location":"api/translator/#nhentai.translator.NekoTranslator.get_balance","title":"<code>get_balance() -&gt; dict</code>","text":"<p>Returns <code>{\"email\", \"plan\", \"level\", \"quota\"}</code>.</p> Source code in <code>nhentai/providers/neko.py</code> <pre><code>def get_balance(self) -&gt; dict:\n    \"\"\"Returns ``{\"email\", \"plan\", \"level\", \"quota\"}``.\"\"\"\n    result: dict = {\"email\": None, \"plan\": PLAN_NAMES[0], \"level\": 0, \"quota\": None}\n\n    if self.token:\n        try:\n            resp = self._request(\n                \"GET\", f\"{BASE_URL}/get-user\", headers=self.headers, timeout=_POLL_TIMEOUT,\n            )\n            if resp.status_code == 200:\n                user = resp.json()\n                result[\"email\"] = user.get(\"email\")\n                level = user.get(\"level\", 0)\n                result[\"level\"] = level\n                result[\"plan\"] = PLAN_NAMES[level] if level &lt; len(PLAN_NAMES) else f\"Level {level}\"\n        except requests.exceptions.RequestException:\n            pass\n\n    try:\n        resp = self._request(\n            \"GET\", f\"{BASE_URL}/get-quota\",\n            headers=self.headers,\n            use_proxy=bool(self._proxy_list),\n            timeout=_POLL_TIMEOUT,\n        )\n        if resp.status_code == 200:\n            result[\"quota\"] = resp.json().get(\"quota\")\n    except requests.exceptions.RequestException:\n        pass\n\n    return result\n</code></pre>"},{"location":"api/translator/#nhentai.translator.NekoTranslator.translate_file","title":"<code>translate_file(image_path: Path, tgt_lang: Language | str = Language.ENGLISH, engine: NekoEngine | str = NekoEngine.DEEPL) -&gt; bytes</code>","text":"<p>Translate an image file and return the result as bytes.</p> Source code in <code>nhentai/providers/neko.py</code> <pre><code>def translate_file(\n    self,\n    image_path: Path,\n    tgt_lang: Language | str = Language.ENGLISH,\n    engine: NekoEngine | str = NekoEngine.DEEPL,\n) -&gt; bytes:\n    \"\"\"\n    Translate an image file and return the result as bytes.\n    \"\"\"\n    image_path = Path(image_path)\n    if not image_path.exists():\n        raise FileNotFoundError(f\"Image not found: {image_path}\")\n    if not image_path.is_file():\n        raise ValueError(f\"Not a file: {image_path}\")\n\n    engine = NekoEngine(engine)\n    tgt_lang = Language(tgt_lang) if not isinstance(tgt_lang, Language) else tgt_lang\n\n    force_proxy = None\n    if not self.token and self._proxy_list:\n        force_proxy = self.find_proxy_with_quota()\n        if force_proxy is None:\n            raise TranslationError(\"All proxies have exhausted anonymous quota.\")\n\n    manga_id = self._upload(image_path, tgt_lang, engine, force_proxy)\n    result_url = self._poll(manga_id)\n    return self._download(result_url)\n</code></pre>"},{"location":"api/translator/#nhentai.translator.NekoTranslator.translate_bytes","title":"<code>translate_bytes(data: bytes, filename: str, tgt_lang: Language | str = Language.ENGLISH, engine: NekoEngine | str = NekoEngine.DEEPL) -&gt; bytes</code>","text":"<p>Translate raw image bytes. Writes to a temp file, translates, returns result bytes.</p> Source code in <code>nhentai/providers/neko.py</code> <pre><code>def translate_bytes(\n    self,\n    data: bytes,\n    filename: str,\n    tgt_lang: Language | str = Language.ENGLISH,\n    engine: NekoEngine | str = NekoEngine.DEEPL,\n) -&gt; bytes:\n    \"\"\"\n    Translate raw image bytes. Writes to a temp file, translates, returns result bytes.\n    \"\"\"\n    suffix = Path(filename).suffix or \".jpg\"\n    with tempfile.NamedTemporaryFile(suffix=suffix, delete=False) as tmp:\n        tmp.write(data)\n        tmp_path = Path(tmp.name)\n    try:\n        return self.translate_file(tmp_path, tgt_lang, engine)\n    finally:\n        os.unlink(tmp_path)\n</code></pre>"},{"location":"api/translator/#comictranslator","title":"ComicTranslator","text":"<p>Client for the Comic-Translator API. Automatically handles guest registration and supports direct URL translation.</p> <pre><code>from nhentai import ComicTranslator, ComicEngine, Language\n\ntranslator = ComicTranslator()\n# Direct URL translation (Faster)\ndata = translator.translate_url(\"https://i.nhentai.net/...\", tgt_lang=Language.ENGLISH, engine=ComicEngine.GEMINI_FLASH)\n</code></pre>"},{"location":"api/translator/#nhentai.translator.ComicTranslator","title":"<code>nhentai.translator.ComicTranslator()</code>","text":"<p>               Bases: <code>BaseTranslator</code></p> <p>ComicTranslator.com API client. Automatically handles guest registration and credit management.</p> Source code in <code>nhentai/providers/comic.py</code> <pre><code>def __init__(self):\n    self.token = None\n    self.credits = 0.0\n    self._session = requests.Session()\n    self._session.headers.update({\n        \"User-Agent\": (\n            \"Mozilla/5.0 (Windows NT 10.0; Win64; x64) \"\n            \"AppleWebKit/537.36 (KHTML, like Gecko) \"\n            \"Chrome/124.0.0.0 Safari/537.36\"\n        ),\n        \"Origin\": \"chrome-extension://liquiddev99@comictranslator.com\",\n        \"Referer\": \"https://comictranslator.com/\",\n    })\n</code></pre>"},{"location":"api/translator/#nhentai.translator.TranslationError","title":"<code>nhentai.translator.TranslationError</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Raised when the translation server reports a failure.</p>"}]}